div#chat
  div#chat-box
  //- maxlength — лишь UX-подсказка; авторитетный лимит на сервере
  //- (chatMaxLength в src/config/game.js). Значения держать синхронными.
  input#cmd(type='text', maxlength='60' autocomplete="off")
