doctype html
html
  head
  body
    div
      p(style='white-space:pre;')
        | Dear #{info.name}, You are banned!
        | Reason: #{info.reason}
        | Time (hours): #{+(info.time / 1000 / 60 / 60).toFixed(2)}
        | Type: #{info.type}
      p #{message}
